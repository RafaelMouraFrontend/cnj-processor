{
  "name": "cnj-processor",
  "version": "1.0.0",
  "description": "Processador de CNJ com Arquitetura Serverless",
  "main": "index.js",
  "scripts": {
    "deploy": "sam build && sam deploy --guided",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.1199.0",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "jest": "^28.1.3"
  }
}